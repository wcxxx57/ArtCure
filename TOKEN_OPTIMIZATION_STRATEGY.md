# Token优化策略 - 每日一次AI推荐

## 🎯 优化目标
- 每用户每日最多1次AI推荐调用
- 基于用户近7天所有心情记录进行分析
- 确保推荐质量和个性化程度

## 🧠 智能触发策略

### 1. 严格的每日限制
**每用户每日只能生成1次AI推荐**
- ✅ 检查今日是否已调用AI（tokenUsed: true）
- ✅ 严格限制每日1次，大幅节省token
- ✅ 确保每次调用都有价值

### 2. 数据要求
**基于近7天所有心情记录**
- 🔥 至少需要3天的心情记录才触发AI
- 🔥 获取用户近7天所有打卡记录（不限数量）
- 🔥 分析完整的心情模式和趋势

### 3. 触发条件
**只在以下情况触发AI生成：**
- 今日未生成AI推荐
- 用户有≥3天的心情记录
- 无有效的AI推荐缓存

## 📊 数据分析增强

### 心情趋势分析
```javascript
// 基于所有7天记录的深度分析
{
  trend: 'improving/declining/stable/fluctuating',
  description: '心情逐渐好转，从3.2分提升到4.1分',
  details: {
    totalRecords: 12,        // 总记录数
    avgMood: 3.6,           // 平均心情
    moodRange: [2, 5],      // 心情范围
    stability: 'moderate',   // 稳定性
    trendDiff: 0.9,         // 趋势差值
    earlierAvg: 3.2,        // 前期平均
    recentAvg: 4.1          // 近期平均
  }
}
```

### AI提示词优化
```javascript
// 包含完整7天数据的提示词
{
  totalRecords: 12,
  moodHistory: "今天: 09:30开心(5分)、18:20平静(4分); 1天前: 低落(2分); 2天前: 一般(3分)...",
  trendAnalysis: "整体趋势：心情逐渐好转。平均心情3.6分，波动范围2-5分，情绪中等波动。",
  requirement: "基于用户完整心情模式推荐3个最适合的疗愈计划..."
}
```

## 📈 预期效果

### Token消耗对比
**优化前（每次打卡触发）：**
- 假设每日1000次心情打卡
- 每次都可能调用AI = 最多1000次调用

**优化后（每日1次限制）：**
- 每用户每日最多1次AI调用
- 假设500活跃用户 = 最多500次调用
- **节省50%以上的token消耗**

### 推荐质量提升
- ✅ 基于完整7天数据，推荐更准确
- ✅ 深度心情趋势分析
- ✅ 个性化程度更高
- ✅ 避免频繁重复推荐

## 🔧 关键参数

```javascript
// 核心限制参数
const CONFIG = {
  DAILY_AI_LIMIT: 1,              // 每日AI调用限制
  MIN_MOOD_RECORDS: 3,            // 最少心情记录要求
  MOOD_HISTORY_DAYS: 7,           // 心情历史分析天数
  CACHE_EXPIRE_DAYS: 2,           // 缓存有效期
  BATCH_SIZE: 10,                 // 批量处理大小
  BATCH_DELAY: 2000              // 批量处理延迟
}
```

## 🚀 实施效果

### 成本控制
- 每用户每日最多消耗1次token
- 可预测的成本支出
- 大幅降低意外消耗

### 用户体验
- 更精准的个性化推荐
- 基于完整数据的深度分析
- 规则推荐作为即时兜底

### 数据价值
- 充分利用用户历史数据
- 深度心情模式识别
- 趋势预测和个性化

## 📊 监控指标

### 关键指标
- **日AI调用次数**：目标 ≤ 活跃用户数
- **推荐覆盖率**：有推荐的活跃用户比例 > 80%
- **数据充足率**：有≥3天记录的用户比例
- **推荐点击率**：衡量推荐质量

### 告警设置
- 日AI调用次数异常增长
- 推荐覆盖率 < 70%
- AI调用失败率 > 5%

通过这种严格的每日限制策略，我们可以在大幅节省成本的同时，提供更高质量的个性化推荐。